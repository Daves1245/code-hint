misc:

api:
* LLM provider
* credentials file / auth & flow
* zod validation
* fix API errors - they currently flood and break the UI
* parallel tool calls in the executing flow's tool loop (see NOTES # tools): [sub-bullets drafted by Claude]
  - canonicalize each call's data footprint (symlinks, rel/abs, parent/child, macOS case-insensitivity) before comparing
  - type calls as static (footprint known up front) or dynamic (unknown until run); dynamic = serialization barrier
  - group into disjoint components by shared data (union-find); run components in parallel, calls within a component serially in arrival order
* handle tool call failures (currently a runTool throw crashes the flow instead of surfacing an error to the model or user)
  - settle each call independently so a rejecting call doesn't drop sibling results (model expects a tool_result per tool_use_id) [drafted by Claude]
  - within a serial component, short-circuit downstream dependents when an upstream call fails (skipped-due-to-dependency, not run on a false premise) [drafted by Claude]

cli:
* hello world
* test suite
* hook chat into app, and render App
* render to the full size of the screen
* render errors
* render chat mode

docs:
* pick and setup a doc system
* stub for each system

evals:
- test suite
- build other systems

include:
- test suite

scripts:
- test suite

store:
* Loadable async transitions

Context engineering:
	- Maintain a knowledge graph of projects and research
	- smart 'read' tool: read, but with an optional natural language 'scope' parameter
	for reducing noise. should the user want to read auth.ts for a specific provider example,
	simply provide 'auth provider <provider> implementation' as the scope. the tool will return
	the relevant portions. if not included, will default to the entire file. also has line-by-line
	and regular grep-like features
	- index files and perform some preliminary retrieval step for some actions. depending on the complexity,
	this can include a lot of information. otherwise, works as 'just-in-time' context.
	- probably add a reranker as well

Research:
	- Google and find best resources to answer query
	- If a great match is found, open it in a tab
	- Quickly spit out a summary of results
	- Optionally add to memory / context layer via 'remember' tool
