{
  // Solution file for `tsc -b`: builds every workspace package in dependency
  // order into a single root-level dist/ (e.g. dist/cli/index.js). Holds no
  // compilerOptions of its own - each package's tsconfig.json is self-contained.
  "files": [],
  "references": [
    { "path": "./agents" },
    { "path": "./api" },
    { "path": "./cli" },
    { "path": "./evals" },
    { "path": "./include" },
    { "path": "./scripts" },
    { "path": "./store" }
  ]
}
